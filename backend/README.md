# Meridian Prediction Agent

This is the prediction backend for Meridian, built using FastAPI and XGBoost.

## Quick Start

Run these exact commands to get started:

```bash
pip install -r requirements.txt
uvicorn main:app --reload
```
